# Runtime dependencies for the casting-defect YOLOv8 pipeline.
#
# torch/torchvision must be installed with a CUDA build to use GPU. On a fresh env:
#   pip install torch==2.4.1 torchvision==0.19.1 --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt

ultralytics==8.4.96
opencv-python>=4.10.0
Pillow>=10.0.0
numpy>=1.24,<2.0

# ONNX export + runtime for the FP32/FP16/INT8 benchmark
onnx>=1.16.0
onnxruntime-gpu>=1.18.0
onnxslim>=0.1.34
